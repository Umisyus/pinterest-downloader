{
  "title": "Actor input schema",
  "description": "This is actor input schema",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "APIFY_TOKEN": {
      "title": "APIFY_TOKEN",
      "type": "string",
      "description": "Token of your Apify account to access datasets",
      "isSecret": true,
      "editor": "textfield"
    },
    "APIFY_USERNAME": {
      "title": "APIFY_USERNAME",
      "type": "string",
      "description": "Your APIFY_USERNAME (customary_dairy, elegant_ostrich, etc.) which can be found in your account profile. Used to access your own datasets",
      "editor": "textfield",
      "nullable": true
    },
    "DATASET_NAME": {
      "title": "DATASET_NAME",
      "type": "string",
      "description": "Name of your dataset can be a random ID as show on the dataset page or a named dataset (which has a name you set)",
      "editor": "textfield"
    },
    "DOWNLOAD_LIMIT": {
      "title": "DOWNLOAD_LIMIT",
      "type": "integer",
      "description": "Limit of items to download from dataset",
      "editor": "number"
    },
    "check_completed": {
      "title": "check_completed",
      "type": "boolean",
      "description": "Chcks for already downlaoded images and only downlaods missing ones",
      "editor": "checkbox"
    }
  },
  "required": [
    "APIFY_TOKEN",
    "DATASET_NAME"
  ]
}