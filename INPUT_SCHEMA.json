{
  "title": "Actor input schema",
  "description": "This is actor input schema",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "APIFY_TOKEN": {
      "title": "APIFY_TOKEN",
      "type": "string",
      "description": "Token of your Apify account to access datasets",
      "isSecret": true,
      "editor": "textfield"
    },
    "ExcludedStores": {
      "title": "ExcludedStores",
      "type": "array",
      "description": "Any key-value store names to exlude from the zipping process",
      "editor": "stringList"
    }
  },
  "required": [
    "APIFY_TOKEN"
  ]
}
