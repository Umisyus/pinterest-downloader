{
  "title": "Actor input schema",
  "description": "This is actor input schema",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "APIFY_TOKEN": {
      "title": "APIFY_TOKEN",
      "type": "string",
      "description": "Token of your Apify account to access datasets. This is NOT required if your dataset is set to be public.",
      "isSecret": true,
      "editor": "textfield"
    },
    "APIFY_USERNAME": {
      "title": "APIFY_USERNAME",
      "type": "string",
      "description": "Your APIFY_USERNAME (customary_dairy, elegant_ostrich, etc.) which can be found in your account profile. Used to access your own datasets",
      "editor": "textfield",
      "nullable": true
    },
    "DATASET_NAME": {
      "title": "DATASET_NAME",
      "type": "string",
      "description": "Name of your dataset can be a random ID as show on the dataset page or a named dataset (which has a name you set)",
      "editor": "textfield"
    },
    "DATASET_URL": {
      "title": "DATASET_URL",
      "type": "string",
      "description": "URL/Link to your dataset. Your dataset MUST be set to public for this option to work!",
      "editor": "textfield",
      "nullable": true
    },
    "DOWNLOAD_LIMIT": {
      "title": "DOWNLOAD_LIMIT",
      "type": "integer",
      "description": "Limit of items to download from dataset",
      "editor": "number",
      "nullable": true
    },
    "ZIP": {
      "title": "Zip downloads?",
      "type": "boolean",
      "description": "Sets whether or not to zip all downloaded files",
      "editor": "boolean",
      "nullable": true
    },
    "required": []
  }
}