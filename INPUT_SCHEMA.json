{
  "title": "Actor input schema",
  "description": "This is actor input schema",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "APIFY_TOKEN": {
      "title": "APIFY_TOKEN",
      "type": "string",
      "description": "Token of your Apify account to access datasets",
      "isSecret": true,
      "editor": "textfield"
    },
    "ExcludedStores": {
      "title": "ExcludedStores",
      "type": "array",
      "description": "Any key-value store names to exlude from the zipping process",
      "editor": "stringList"
    },
    "IncludedStores": {
      "title": "IncludedStores",
      "type": "array",
      "description": "Any key-value store names to include in the zipping process",
      "editor": "stringList"
    },
    "FILES_PER_ZIP": {
      "title": "FILES_PER_ZIP",
      "type": "string",
      "description": "Number of items in each ZIP file",
      "editor": "textfield"
    },
    "MAX_SIZE_MB": {
      "title": "MAX_SIZE_MB",
      "type": "string",
      "description": "Size in Megabytes of each ZIP file",
      "editor": "textfield"
    }
  },
  "required": [
    "APIFY_TOKEN"
  ]
}