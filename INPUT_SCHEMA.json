{
  "title": "KVS Zip Input Definition",
  "description": "This is the KVS zip Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "APIFY_TOKEN": {
      "title": "APIFY_TOKEN",
      "type": "string",
      "description": "Token of your Apify account to access datasets",
      "isSecret": true,
      "editor": "textfield"
    },
    "APIFY_USERNAME": {
      "title": "Apify Username",
      "type": "string",
      "description": "User name of your Apify account to access datasets",
      "isSecret": false,
      "editor": "textfield"
    },
    "ZIP_IncludedStores": {
      "title": "ZIP_IncludedStores",
      "type": "array",
      "description": "Any key-value store names to include in the zipping process. If empty, all stores will be included.",
      "default": [
        "INPUT",
        "default"
      ],
      "prefill": [
        "INPUT",
        "default"
      ],
      "editor": "stringList"
    },
    "ZIP_ExcludedStores": {
      "title": "ZIP_ExcludedStores",
      "type": "array",
      "description": "Any key-value store names to exlude from the zipping process. If empty, no stores will be excluded.",
      "editor": "stringList",
      "prefill": "INPUT\ndefault"
    },
    "FILES_PER_ZIP": {
      "title": "FILES_PER_ZIP",
      "type": "integer",
      "description": "Number of items in each ZIP file",
      "editor": "number"
    },
    "MAX_SIZE_MB": {
      "title": "MAX_SIZE_MB",
      "type": "integer",
      "description": "Size in Megabytes of each ZIP file",
      "editor": "number"
    },
    "DOWNLOAD_CONCURRENCY": {
      "title": "How many concurrent downloads?",
      "type": "integer",
      "description": "How many concurrent downloads at once? Use '1' to download 1 file at a time.",
      "editor": "number"
    },
    "DOWNLOAD_DELAY": {
      "title": "How long to wait between downloads?",
      "type": "integer",
      "description": "How long to wait between downloads in milliseconds? Use 0 to download without delay.",
      "editor": "number"
    }
  },
  "required": [
    "APIFY_TOKEN"
  ]
}
